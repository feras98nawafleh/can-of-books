import { Component } from "react"
import { Button } from "react-bootstrap"

export default class LoginButton extends Component {
  render() {
    return <Button variant="outline-info">Login</Button>
  }
}
